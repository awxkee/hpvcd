[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25.10", default-features = false, features = ["jpeg", "png"] }
hpvcd = { path = "../" }
libheif-rs = { version = "2.6", default-features = false, features = ["v1_21"] }